# Report: v0.8.0 release (merge + push + GitHub release)

operation_id: op-260827-release-v080

## Status: DONE

## Summary

Bumped version to 0.8.0 across all surfaces, merged ui-restyle-header-drag
into main, rebuilt the release artifact, pushed main, tagged v0.8.0, and
published the GitHub release with the zip asset. Full vitest suite stayed
green (119/119) through the bump and merge. No conflicts, no test
failures, no hook rejections.

## Version surfaces verified (all agree at 0.8.0)

- `package.json`
- `src-tauri/Cargo.toml`
- `src-tauri/tauri.conf.json`
- `src-tauri/Cargo.lock` (own `my-translator` package entry only — refreshed
  via `cargo check`, not `generate-lockfile`; all 593 dependency entries
  unchanged, verified by diff)

## Merge

- Commit on branch: `6ce4a14` — chore(release): bump version to 0.8.0
- Merge commit on main: **`02738bcd65f70d53fde1570392c635418df0fffd`**
  (`git merge --no-ff`, matching repo history style)
- 29 files changed, 4063 insertions(+), 403 deletions(-); zero conflicts
- `ui-restyle-header-drag` branch left intact (not deleted, per spec)

## Test results

vitest, run Windows-side (`mcp__cmd__cmd_run` → `npm test`), both before
merge (on the branch, post-bump) and confirmed unchanged after merge (same
files): **119/119 passed**, 20 test files. Stderr in the log is expected
(tests intentionally exercising error-path console.error branches).
Cargo: no test run needed (no Rust *behavior* changed by the bump — only
the version string; `cargo check` compiled clean, 7 pre-existing unrelated
warnings only).

## Release artifact (rebuilt on main, post-merge)

- `D:\WORKSPACES\PERSONAL\my-translator\src-tauri\target\release\my-translator.exe`
  — 11,372,032 bytes, mtime 2026-08-27 17:00:55 (epoch 1787824855)
- `D:\WORKSPACES\PERSONAL\my-translator\dist\MyTranslator-v0.8.0-windows-x64.zip`
  — 3,708,210 bytes, mtime 2026-08-27 17:00:56 (epoch 1787824856)
- Freshness: newest source mtime (frontend `src/**` + Rust `src-tauri/src/**`/
  Cargo.toml/Cargo.lock) is `src/styles/main.css` at epoch 1787824811
  (touched by the merge checkout). exe epoch 1787824855 > 1787824811 —
  exe postdates all source by ~44s.

## Push / tag

- `git push origin main`: `728f8b2..02738bc main -> main` (accepted, no
  pre-push hook rejection — confirms no session-link/co-author trailers
  slipped into any commit)
- Tag `v0.8.0` created annotated on the merge commit, pushed
- `git rev-list -n1 v0.8.0` == `git rev-parse main` == `02738bcd65f70d53fde1570392c635418df0fffd`
- Verified `local main == origin/main` after push (both resolve to `02738bc`)

## GitHub release

- URL: https://github.com/miken90/my-translator/releases/tag/v0.8.0
- Created via `gh release create` after switching the active `gh` account
  to `miken90` (was `canhnguyenhd`; SSH remote push already worked under
  the `self.github.com` host alias mapped to miken90's key)
- Notes: extracted only the v0.8.0 section from `RELEASE_NOTES.md` (matches
  how the v0.7.0 release was published — confirmed via `gh release view v0.7.0`)
- `gh release view v0.8.0 --json` confirms: not draft, not prerelease,
  author `miken90`, asset `MyTranslator-v0.8.0-windows-x64.zip` at 3,708,210
  bytes — exact match to the local rebuilt zip's size

## Unresolved questions

None.
